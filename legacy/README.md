# Legacy Python implementation

This directory contains the original Python implementation of `copilot-cost` (pre-community).

It is kept for one release cycle for reference. Users should install the npm package instead:

```sh
npx copilot-cost install
```

This legacy implementation will be removed in a future major version.
